# @fractaal/pi-codex-usage-info

Canonical package for Ben's personal Codex/OpenAI usage statusline hook.

It preserves the original statusline behavior and lifecycle hooks:

- `session_start`
- `session_tree`
- `model_select`
- `session_shutdown`
